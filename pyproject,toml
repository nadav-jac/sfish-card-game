[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sfish"
version = "0.0.1"
dependencies = []
python = ">=3.11"

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-mock",
]
